extends Node


# Called when the node enters the scene tree for the first time.
func _ready():
# get card pool object from cards.json
# select random cards from pool, each sequence cannot have duplicates. random selections are removed from pool



# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
